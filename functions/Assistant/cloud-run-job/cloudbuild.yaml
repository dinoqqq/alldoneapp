steps:
    - name: gcr.io/cloud-builders/docker
      args:
          - build
          - --file
          - Assistant/cloud-run-job/Dockerfile
          - --tag
          - ${_IMAGE}
          - .
images:
    - ${_IMAGE}
